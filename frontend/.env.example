# ============================================================
# 前端环境变量模板
#
# 使用方法：
#   cp .env.example .env
#
# 只有 VITE_ 前缀的变量会被 Vite 注入到前端代码里
# （见 api/client.ts 里的 import.meta.env.VITE_API_BASE_URL）。
#
# 注意：前端的环境变量是【打包进产物】的，浏览器里能看到。
# 所以这里只能放地址这类公开信息，绝不要放 API Key、
# 数据库密码之类的秘密 —— 那等于直接公布出去。
# ============================================================

# 后端地址。开发时后端默认跑在 8000 端口。
VITE_API_BASE_URL=http://127.0.0.1:8000
